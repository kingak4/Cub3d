#include "mlx/mlx.h"
#include "cub3d.h"

void raycast(int w);

int	main(void)
{
//	void	*mlx;
//	void	*mlx_win;

//	mlx = mlx_init();
//	mlx_win = mlx_new_window(mlx, 1920, 1080, "Hello world!");
	int w = 1920;
	raycast(w);
//	mlx_loop(mlx);
}
